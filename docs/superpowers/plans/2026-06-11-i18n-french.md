# i18n French Language Support — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add French as a second language to edwt.ca using next-intl in cookie mode with a topbar language toggle — no URL changes.

**Architecture:** next-intl configured without URL prefixes. Locale stored in a `NEXT_LOCALE` cookie, resolved by middleware (cookie → Accept-Language → "en" fallback). Translation strings live in `messages/en.json` and `messages/fr.json`, organized by namespace. Client components use `useTranslations()`, server components use `getTranslations()`. A toggle button in the topbar sets the cookie and refreshes.

**Tech Stack:** Next.js 16 (App Router), React 19, next-intl, Tailwind 4, Sentry

**Design spec:** `docs/plans/i18n-design.md`

---

## File Map

| Action | File | Responsibility |
|--------|------|----------------|
| Create | `messages/en.json` | English translation strings (all namespaces) |
| Create | `messages/fr.json` | French translation strings (all namespaces) |
| Create | `src/i18n/config.ts` | Locale constants and types |
| Create | `src/i18n/request.ts` | Server-side locale resolution for next-intl |
| Create | `src/middleware.ts` | Cookie-based locale detection, no URL rewrites |
| Modify | `next.config.ts` | Wrap with `createNextIntlPlugin()` |
| Modify | `src/app/layout.tsx` | Dynamic `<html lang>`, `NextIntlClientProvider`, translated metadata |
| Modify | `src/app/app-topbar.tsx` | Translate nav labels, add locale toggle button |
| Modify | `src/app/data.ts` | Change `severityLabel()` and `facilityWaitStatusLabel()` to return translation keys |
| Modify | `src/app/page.tsx` | Translate JSON-LD descriptions |
| Modify | `src/app/page-client.tsx` | Replace all hardcoded UI strings with `useTranslations()` calls |
| Modify | `src/app/map/page.tsx` | Translate metadata |
| Modify | `src/app/map/map-client.tsx` | Replace hardcoded UI strings with `useTranslations()` calls |
| Modify | `src/app/analytics/page.tsx` | Replace all hardcoded UI strings with `getTranslations()` / `useTranslations()` calls |
| Modify | `src/app/analytics/analytics-charts.tsx` | Replace chart titles/labels with `useTranslations()` calls |

---

### Task 1: Install next-intl and create config

**Files:**
- Create: `src/i18n/config.ts`
- Modify: `package.json` (via pnpm)

- [ ] **Step 1: Install next-intl**

```bash
pnpm add next-intl
```

Expected: Package added to `dependencies` in `package.json`.

- [ ] **Step 2: Create locale config**

Create `src/i18n/config.ts`:

```ts
export const locales = ["en", "fr"] as const;
export type Locale = (typeof locales)[number];
export const defaultLocale: Locale = "en";
```

- [ ] **Step 3: Commit**

```bash
git add src/i18n/config.ts package.json pnpm-lock.yaml
git commit -m "feat(i18n): install next-intl and add locale config"
```

---

### Task 2: Create English translation file

**Files:**
- Create: `messages/en.json`

- [ ] **Step 1: Create `messages/en.json`**

```json
{
  "nav": {
    "facilities": "Facilities",
    "map": "Map",
    "analytics": "Analytics",
    "liveWaitTimes": "Live wait times"
  },
  "common": {
    "closed": "Closed",
    "open": "Open",
    "shortWait": "Short wait",
    "moderateWait": "Moderate wait",
    "longWait": "Long wait",
    "noData": "No data",
    "close": "Close",
    "updated": "Updated {time}",
    "emergency": "Emergency",
    "upcc": "UPCC"
  },
  "facilities": {
    "heroTitle": "Find the <em>shortest</em> ED wait near you.",
    "heroSub": "Live wait times for emergency departments and walk-in clinics from the live EDWT feed. Updated every few minutes.",
    "walkInUpcc": "Walk-in / UPCC",
    "pediatric": "Pediatric",
    "allFacilities": "All facilities",
    "openNow": "Open now",
    "shortestWait": "Shortest wait",
    "closestFirst": "Closest first",
    "nameAZ": "Name A-Z",
    "sortWaitDesc": "Prioritize sites reporting the lowest current wait.",
    "sortDistDesc": "Show the nearest open facilities first.",
    "sortNameDesc": "Browse facilities alphabetically.",
    "directions": "Directions",
    "call": "Call",
    "details": "Details",
    "fullDetails": "Full details",
    "website": "Website",
    "visitWebsite": "Visit {name} website",
    "waiting": "{count} waiting",
    "onDuty": "{count} on duty",
    "kmAway": "{distance} km",
    "preciseLocation": "Precise location",
    "approximateLocation": "Approximate location",
    "usePreciseLocation": "Use precise location",
    "gettingLocation": "Getting precise location",
    "preciseEnabled": "Precise location enabled",
    "locationDenied": "Location permission was denied",
    "locationUnavailable": "Precise location is unavailable",
    "gpsNeedsHttps": "GPS needs HTTPS on mobile devices",
    "noFacilitiesOpen": "No facilities are currently reporting as open.",
    "noWaitTimes": "No posted wait times are available right now.",
    "feedPaused": "The live feed may be paused or every site in range is closed.",
    "noWaitData": "Open facilities may still be accepting patients, but the live feed has not posted wait data.",
    "noWaitPosted": "No wait posted",
    "noMatch": "No facilities match this filter.",
    "showAll": "Show all facilities",
    "sortFacilities": "Sort facilities",
    "chooseOrder": "Choose list order",
    "sort": "Sort",
    "sortedBy": "Sorted by",
    "facilityFilters": "Facility filters",
    "recommendedForYou": "Recommended for you",
    "shortestReportedWait": "Shortest reported wait among open facilities — about a {wait} expected wait",
    "withWaiting": "with <b>{count} people</b> in the waiting room",
    "kmFromLocation": "~{distance} km from your location.",
    "cliniciansOnDuty": "{count} clinicians on duty",
    "statShortestWait": "Shortest wait",
    "statClosestOpen": "Closest open",
    "statAverageWait": "Average wait now",
    "statOpenNow": "Open right now",
    "acrossOpen": "across {count} open",
    "facilitiesReporting": "facilities reporting",
    "emergencyBannerStrong": "If this is a life-threatening emergency, call 9-1-1.",
    "emergencyBannerBody": "Chest pain, severe bleeding, stroke symptoms, or difficulty breathing need immediate care. For non-urgent health advice, dial {healthLink} to reach a registered nurse 24/7.",
    "nurseAdvice": "Call {healthLink} for nurse advice, or {emergencyLink} if this is life-threatening.",
    "whatToExpect": "What to expect",
    "expectOpen": "The reported wait is the latest published wait-time reading for this facility. Sicker patients are seen first, so the live wait can change quickly.",
    "expectWaiting": "Right now there are <b>{count} people</b> in the waiting room.",
    "expectNoWait": "This facility is open, but no wait time is currently posted.",
    "expectClosed": "This facility is currently closed. It will reopen at the next scheduled time.",
    "address": "Address",
    "hours": "Hours",
    "callPhone": "Call {phone}",
    "advice01Title": "Not sure where to go?",
    "advice01Body": "Call 8-1-1 to talk with a registered nurse 24/7. They can help you decide whether you need the ED, a clinic, or self-care at home.",
    "advice01Link": "Call 8-1-1 →",
    "advice02Title": "How wait time is measured",
    "advice02Body": "Wait time is the duration for 9 out of 10 patients to be seen by a physician — not the full visit length. Sicker patients are seen first.",
    "advice03Title": "What to bring",
    "advice03Body": "Bring your BC Services Card, a list of medications, and something to keep you occupied. Eat and drink lightly unless told otherwise.",
    "footerTagline": "Live emergency wait times · Lower Mainland, BC",
    "serviceStatus": "Service status",
    "footnote": "Wait times are estimates only and update every few minutes. If your condition worsens while waiting, tell the triage nurse. This site is an independent demo and not affiliated with any health authority.",
    "statsSummary": "<b>{openCount}</b> of {totalCount} facilities open · shortest <b>{shortest}</b> · average <b>{average}</b>"
  },
  "map": {
    "title": "Facility Map",
    "description": "Interactive map of emergency departments and urgent care centres in the Lower Mainland, BC with live wait times and directions.",
    "reporting": "{count} reporting",
    "nearbyFacilities": "Nearby facilities",
    "mapSidebarDesc": "Wait-time markers are colored by severity. Pick a facility to inspect the current wait, distance, and directions.",
    "shortestOpenWait": "Shortest open wait",
    "highestPressure": "Highest pressure",
    "noPostedWaits": "No posted waits",
    "noOpenFacilities": "No open facilities",
    "facilityIsClosed": "Facility is closed",
    "distance": "Distance",
    "waiting": "Waiting",
    "onDuty": "On duty",
    "topPressureNote": "This site is currently in the top pressure group.",
    "routing": "Routing...",
    "directions": "Directions",
    "centerMap": "Center map",
    "routeFrom": "Route from {origin}: {distance} km · {duration} min drive",
    "locationRequired": "Precise location is required to show directions.",
    "showLocationRequired": "Precise location is required to show your location on the map.",
    "findingLocation": "Finding your location",
    "showYourLocation": "Show your location",
    "mapUnavailable": "Map unavailable",
    "yourLocation": "Your location"
  },
  "analytics": {
    "heroTitle": "Wait-time analytics",
    "heroSubtitle": "A system-level view of current pressure, sustained risk, coverage quality, and care-type trends across tracked facilities.",
    "liveAnalytics": "Live wait-time analytics",
    "dataWindow": "Data window",
    "latestSourceReading": "Latest source reading",
    "snapshotMode": "Snapshot mode",
    "currentFacilitySnapshot": "Current facility snapshot",
    "facilitiesTracked": "Facilities tracked",
    "locationsProducedReadings": "{count} locations produced wait-time readings",
    "readingsCaptured": "Readings captured",
    "includeWaitMinutes": "{percent} include wait-time minutes",
    "pollArchive": "Poll archive",
    "medianCadence": "Median cadence {seconds} seconds",
    "archiveNotPresent": "Archive table not present",
    "freshness": "Freshness",
    "p95SourceLag": "P95 source lag {minutes} minutes",
    "sourceLagDisabled": "Historical source-lag query disabled",
    "executiveReadout": "Executive readout",
    "whatNeedsAttention": "What needs attention now",
    "readoutDesc": "Fast scan of current wait-time pressure, sustained averages, and structural data gaps.",
    "currentHighestPressure": "Current highest pressure",
    "latestWaitAt": "Latest wait time {wait} at {time} PT",
    "highestSustainedAverage": "Highest sustained average",
    "avgMedianWait": "Average wait time {avg}, median wait time {median}",
    "edAccessGap": "ED access gap",
    "longerMedianWait": "{wait} longer median wait time",
    "edVsUpccMedian": "ED median wait time {ed} vs UPCC median wait time {upcc}",
    "coverageNote": "Coverage note",
    "locationsWithoutReadings": "{count} locations without wait-time readings",
    "coverageDetail": "Most are configured with wait-time display hidden, so treat this as product state rather than missing telemetry.",
    "careType": "Care type",
    "edVsUpcc": "ED vs UPCC",
    "edVsUpccDesc": "Median wait time and P90 wait time stay separate so tail pressure does not disappear inside an average.",
    "medianWaitTime": "Median wait time",
    "p90WaitTime": "P90 wait time",
    "medianWaitLabel": "Median wait time {wait}",
    "p90WaitLabel": "P90 wait time {wait}",
    "currentWaitPressure": "Current wait-time pressure",
    "currentWaitPressureDesc": "Latest reading per facility, sorted by wait time.",
    "aboveBaselineSignals": "Above-baseline signals",
    "aboveBaselineDesc": "Current wait time versus each site's own short history.",
    "noAboveBaseline": "No above-baseline signals in the current data window.",
    "sustainedHighWaitTimes": "Sustained high wait times",
    "sustainedHighDesc": "Facilities with at least 50 readings, ranked by average wait time.",
    "volatility": "Volatility",
    "volatilityDesc": "High standard deviation means the facility swings more than its peers.",
    "facility": "Facility",
    "type": "Type",
    "waitTime": "Wait time",
    "estimatedLengthOfStay": "Estimated length of stay",
    "observed": "Observed",
    "currentWait": "Current wait time",
    "averageWait": "Average wait time",
    "deltaFromAverage": "Delta from average",
    "zScore": "Z-score",
    "medianWait": "Median wait time",
    "maxWait": "Max wait time",
    "standardDeviation": "Standard deviation",
    "readings": "Readings",
    "locationsWithoutReadingsTitle": "Locations without wait-time readings",
    "locationsWithoutReadingsDesc": "Useful for data quality checks and product-state audits.",
    "showWaitTimes": "Show wait times",
    "fallbackText": "Fallback wait-time text",
    "everyLocationHasReadings": "Every tracked location has at least one wait-time reading.",
    "databaseUnavailable": "Database unavailable",
    "dbErrorTitle": "The analytics page could not read Postgres.",
    "dbErrorDesc": "Check DATABASE_URL or the environment file used to start Next, then refresh this page.",
    "backToFacilities": "Back to facilities",
    "shellTitle": "Analytics page is loading without data.",
    "shellDesc": "The route rendered successfully without database queries. Data panels are temporarily disabled while production access is verified.",
    "method1Title": "Median wait time and P90 wait time first",
    "method1Body": "Average wait time is useful, but median wait time and P90 wait time are better for public pressure and tail-risk decisions.",
    "method2Title": "Baseline per site",
    "method2Body": "Each facility has its own normal range, so alerts compare current wait times to that site's own history.",
    "method3Title": "Separate structural gaps",
    "method3Body": "Locations with hidden wait-time display should be shown as product state, not confused with broken telemetry.",
    "yes": "yes",
    "no": "no"
  },
  "charts": {
    "currentAccessPressure": "Current access pressure",
    "currentAccessPressureDesc": "Longest current wait times, severity-colored by threshold.",
    "edVsUpccTrend": "ED vs UPCC trend",
    "edVsUpccTrendDesc": "Median wait time and P90 wait time by care type, shown separately for clearer tail risk.",
    "sustainedFacilityPressure": "Sustained facility pressure",
    "sustainedFacilityPressureDesc": "Facilities ranked by P90 wait time. Median wait time shows typical pressure; P90 wait time shows bad-day pressure.",
    "currentVsMedian": "Current wait time vs usual median",
    "currentVsMedianDesc": "Neutral dot is site median wait time; colored dot is current wait time. The line shows the gap.",
    "tailRiskMap": "Tail-risk map",
    "tailRiskMapDesc": "Median wait time vs P90 wait time by facility. Upper-right sites are typically slow and painful in the tail.",
    "waitDistribution": "Wait-time distribution",
    "waitDistributionDesc": "How many readings land in each wait-time bucket, useful for long-tail pressure checks.",
    "facilityHourPattern": "Facility-hour pattern",
    "facilityHourPatternDesc": "Top sustained-pressure facilities by local hour. Darker means higher average wait time.",
    "coverageVolume": "Coverage volume",
    "coverageVolumeDesc": "Readings per facility. Low-volume facilities should be interpreted cautiously.",
    "under1h": "under 1h",
    "over2h": "2h+",
    "over4h": "4h+",
    "over6h": "6h+",
    "edMedianWait": "ED median wait time",
    "edP90Wait": "ED P90 wait time",
    "upccMedianWait": "UPCC median wait time",
    "upccP90Wait": "UPCC P90 wait time",
    "currentWaitTime": "Current wait time"
  },
  "metadata": {
    "siteTitle": "EDWT · Lower Mainland ED & UPCC Wait Times",
    "siteDescription": "Live wait times for emergency departments and urgent care centres across the Lower Mainland, BC. Updated every minute from the edwaittimes.ca feed.",
    "ogTitle": "EDWT · Live ED & UPCC Wait Times",
    "analyticsTitle": "Wait-Time Analytics",
    "analyticsDescription": "System-level analytics for Lower Mainland ED and UPCC wait times — trends, heatmaps, tail-risk, and coverage across tracked facilities.",
    "mapTitle": "Facility Map",
    "mapDescription": "Interactive map of emergency departments and urgent care centres in the Lower Mainland, BC with live wait times and directions.",
    "jsonLdWebsiteDesc": "Live wait times for emergency departments and urgent care centres across the Lower Mainland, BC. Updated every 60 seconds from official BC health authority data.",
    "jsonLdAppDesc": "Free real-time emergency department and urgent care wait time tracker for the Lower Mainland, British Columbia. Shows live wait times, physician counts, and patient volumes for 12+ hospitals and UPCCs. Data sourced from the official edwaittimes.ca feed every 60 seconds.",
    "jsonLdFeatureList": "Live wait times, GPS distance sorting, Interactive map, Historical analytics, Progressive Web App, 60-second refresh"
  }
}
```

- [ ] **Step 2: Verify JSON is valid**

```bash
node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8')); console.log('valid')"
```

Expected: `valid`

- [ ] **Step 3: Commit**

```bash
git add messages/en.json
git commit -m "feat(i18n): add English translation file"
```

---

### Task 3: Create French translation file

**Files:**
- Create: `messages/fr.json`

- [ ] **Step 1: Create `messages/fr.json`**

Same structure as `en.json` with French translations. Use accurate Canadian French. Here is the full file:

```json
{
  "nav": {
    "facilities": "Établissements",
    "map": "Carte",
    "analytics": "Analytique",
    "liveWaitTimes": "Temps d'attente en direct"
  },
  "common": {
    "closed": "Fermé",
    "open": "Ouvert",
    "shortWait": "Attente courte",
    "moderateWait": "Attente modérée",
    "longWait": "Attente longue",
    "noData": "Aucune donnée",
    "close": "Fermer",
    "updated": "Mis à jour {time}",
    "emergency": "Urgence",
    "upcc": "CSUP"
  },
  "facilities": {
    "heroTitle": "Trouvez l'attente <em>la plus courte</em> aux urgences près de chez vous.",
    "heroSub": "Temps d'attente en direct pour les urgences et les cliniques sans rendez-vous du flux EDWT. Mis à jour toutes les quelques minutes.",
    "walkInUpcc": "Sans rendez-vous / CSUP",
    "pediatric": "Pédiatrique",
    "allFacilities": "Tous les établissements",
    "openNow": "Ouvert maintenant",
    "shortestWait": "Attente la plus courte",
    "closestFirst": "Le plus proche",
    "nameAZ": "Nom A-Z",
    "sortWaitDesc": "Prioriser les sites avec l'attente la plus courte.",
    "sortDistDesc": "Afficher les établissements ouverts les plus proches.",
    "sortNameDesc": "Parcourir les établissements par ordre alphabétique.",
    "directions": "Itinéraire",
    "call": "Appeler",
    "details": "Détails",
    "fullDetails": "Tous les détails",
    "website": "Site web",
    "visitWebsite": "Visiter le site web de {name}",
    "waiting": "{count} en attente",
    "onDuty": "{count} en service",
    "kmAway": "{distance} km",
    "preciseLocation": "Localisation précise",
    "approximateLocation": "Localisation approximative",
    "usePreciseLocation": "Utiliser la localisation précise",
    "gettingLocation": "Obtention de la localisation précise",
    "preciseEnabled": "Localisation précise activée",
    "locationDenied": "La permission de localisation a été refusée",
    "locationUnavailable": "La localisation précise n'est pas disponible",
    "gpsNeedsHttps": "Le GPS nécessite HTTPS sur les appareils mobiles",
    "noFacilitiesOpen": "Aucun établissement ne signale être ouvert actuellement.",
    "noWaitTimes": "Aucun temps d'attente affiché n'est disponible pour le moment.",
    "feedPaused": "Le flux en direct est peut-être en pause ou chaque site à portée est fermé.",
    "noWaitData": "Les établissements ouverts acceptent peut-être encore des patients, mais le flux en direct n'a pas publié de données d'attente.",
    "noWaitPosted": "Aucun temps affiché",
    "noMatch": "Aucun établissement ne correspond à ce filtre.",
    "showAll": "Afficher tous les établissements",
    "sortFacilities": "Trier les établissements",
    "chooseOrder": "Choisir l'ordre de la liste",
    "sort": "Trier",
    "sortedBy": "Trié par",
    "facilityFilters": "Filtres d'établissements",
    "recommendedForYou": "Recommandé pour vous",
    "shortestReportedWait": "Attente la plus courte parmi les établissements ouverts — environ {wait} d'attente prévue",
    "withWaiting": "avec <b>{count} personnes</b> dans la salle d'attente",
    "kmFromLocation": "~{distance} km de votre position.",
    "cliniciansOnDuty": "{count} cliniciens en service",
    "statShortestWait": "Attente la plus courte",
    "statClosestOpen": "Le plus proche ouvert",
    "statAverageWait": "Attente moyenne actuelle",
    "statOpenNow": "Ouverts maintenant",
    "acrossOpen": "parmi {count} ouverts",
    "facilitiesReporting": "établissements signalés",
    "emergencyBannerStrong": "S'il s'agit d'une urgence vitale, composez le 9-1-1.",
    "emergencyBannerBody": "Douleur thoracique, saignement sévère, symptômes d'AVC ou difficulté à respirer nécessitent des soins immédiats. Pour des conseils de santé non urgents, composez le {healthLink} pour parler à une infirmière 24h/24.",
    "nurseAdvice": "Appelez le {healthLink} pour des conseils infirmiers, ou le {emergencyLink} si c'est une urgence vitale.",
    "whatToExpect": "À quoi s'attendre",
    "expectOpen": "L'attente signalée est la dernière lecture de temps d'attente publiée pour cet établissement. Les patients plus gravement atteints sont vus en premier, l'attente en direct peut donc changer rapidement.",
    "expectWaiting": "Il y a actuellement <b>{count} personnes</b> dans la salle d'attente.",
    "expectNoWait": "Cet établissement est ouvert, mais aucun temps d'attente n'est actuellement affiché.",
    "expectClosed": "Cet établissement est actuellement fermé. Il rouvrira à la prochaine heure prévue.",
    "address": "Adresse",
    "hours": "Heures",
    "callPhone": "Appeler {phone}",
    "advice01Title": "Vous ne savez pas où aller ?",
    "advice01Body": "Composez le 8-1-1 pour parler avec une infirmière autorisée 24h/24, 7j/7. Elle peut vous aider à décider si vous avez besoin des urgences, d'une clinique ou de soins à domicile.",
    "advice01Link": "Appeler le 8-1-1 →",
    "advice02Title": "Comment le temps d'attente est mesuré",
    "advice02Body": "Le temps d'attente est la durée pour que 9 patients sur 10 soient vus par un médecin — pas la durée totale de la visite. Les patients plus malades sont vus en premier.",
    "advice03Title": "Quoi apporter",
    "advice03Body": "Apportez votre carte de services de la C.-B., une liste de médicaments et de quoi vous occuper. Mangez et buvez légèrement sauf indication contraire.",
    "footerTagline": "Temps d'attente aux urgences en direct · Lower Mainland, C.-B.",
    "serviceStatus": "État du service",
    "footnote": "Les temps d'attente sont des estimations seulement et sont mis à jour toutes les quelques minutes. Si votre état s'aggrave pendant l'attente, informez l'infirmière de triage. Ce site est une démo indépendante et n'est affilié à aucune autorité sanitaire.",
    "statsSummary": "<b>{openCount}</b> de {totalCount} établissements ouverts · attente la plus courte <b>{shortest}</b> · moyenne <b>{average}</b>"
  },
  "map": {
    "title": "Carte des établissements",
    "description": "Carte interactive des urgences et centres de soins urgents du Lower Mainland, C.-B. avec les temps d'attente en direct et les itinéraires.",
    "reporting": "{count} signalés",
    "nearbyFacilities": "Établissements à proximité",
    "mapSidebarDesc": "Les marqueurs de temps d'attente sont colorés par gravité. Sélectionnez un établissement pour consulter l'attente actuelle, la distance et l'itinéraire.",
    "shortestOpenWait": "Attente ouverte la plus courte",
    "highestPressure": "Pression la plus élevée",
    "noPostedWaits": "Aucun temps affiché",
    "noOpenFacilities": "Aucun établissement ouvert",
    "facilityIsClosed": "L'établissement est fermé",
    "distance": "Distance",
    "waiting": "En attente",
    "onDuty": "En service",
    "topPressureNote": "Ce site est actuellement dans le groupe de pression le plus élevé.",
    "routing": "Calcul de l'itinéraire...",
    "directions": "Itinéraire",
    "centerMap": "Centrer la carte",
    "routeFrom": "Itinéraire depuis {origin} : {distance} km · {duration} min en voiture",
    "locationRequired": "La localisation précise est requise pour afficher l'itinéraire.",
    "showLocationRequired": "La localisation précise est requise pour afficher votre position sur la carte.",
    "findingLocation": "Recherche de votre position",
    "showYourLocation": "Afficher votre position",
    "mapUnavailable": "Carte indisponible",
    "yourLocation": "Votre position"
  },
  "analytics": {
    "heroTitle": "Analytique des temps d'attente",
    "heroSubtitle": "Vue systémique de la pression actuelle, du risque soutenu, de la qualité de couverture et des tendances par type de soins dans les établissements suivis.",
    "liveAnalytics": "Analytique des temps d'attente en direct",
    "dataWindow": "Fenêtre de données",
    "latestSourceReading": "Dernière lecture source",
    "snapshotMode": "Mode instantané",
    "currentFacilitySnapshot": "Instantané actuel des établissements",
    "facilitiesTracked": "Établissements suivis",
    "locationsProducedReadings": "{count} emplacements ont produit des lectures de temps d'attente",
    "readingsCaptured": "Lectures capturées",
    "includeWaitMinutes": "{percent} incluent les minutes d'attente",
    "pollArchive": "Archive des sondages",
    "medianCadence": "Cadence médiane {seconds} secondes",
    "archiveNotPresent": "Table d'archive non présente",
    "freshness": "Fraîcheur",
    "p95SourceLag": "Décalage source P95 {minutes} minutes",
    "sourceLagDisabled": "Requête de décalage source historique désactivée",
    "executiveReadout": "Résumé exécutif",
    "whatNeedsAttention": "Ce qui nécessite attention maintenant",
    "readoutDesc": "Analyse rapide de la pression actuelle des temps d'attente, des moyennes soutenues et des lacunes structurelles des données.",
    "currentHighestPressure": "Pression actuelle la plus élevée",
    "latestWaitAt": "Dernier temps d'attente {wait} à {time} HP",
    "highestSustainedAverage": "Moyenne soutenue la plus élevée",
    "avgMedianWait": "Temps d'attente moyen {avg}, temps d'attente médian {median}",
    "edAccessGap": "Écart d'accès aux urgences",
    "longerMedianWait": "{wait} d'attente médiane plus longue",
    "edVsUpccMedian": "Attente médiane urgences {ed} vs CSUP {upcc}",
    "coverageNote": "Note de couverture",
    "locationsWithoutReadings": "{count} emplacements sans lectures de temps d'attente",
    "coverageDetail": "La plupart sont configurés avec l'affichage du temps d'attente masqué, considérez cela comme l'état du produit plutôt que de la télémétrie manquante.",
    "careType": "Type de soins",
    "edVsUpcc": "Urgences vs CSUP",
    "edVsUpccDesc": "Le temps d'attente médian et le P90 restent séparés pour que la pression de queue ne disparaisse pas dans une moyenne.",
    "medianWaitTime": "Temps d'attente médian",
    "p90WaitTime": "Temps d'attente P90",
    "medianWaitLabel": "Temps d'attente médian {wait}",
    "p90WaitLabel": "Temps d'attente P90 {wait}",
    "currentWaitPressure": "Pression actuelle des temps d'attente",
    "currentWaitPressureDesc": "Dernière lecture par établissement, triée par temps d'attente.",
    "aboveBaselineSignals": "Signaux au-dessus de la référence",
    "aboveBaselineDesc": "Temps d'attente actuel par rapport à l'historique propre de chaque site.",
    "noAboveBaseline": "Aucun signal au-dessus de la référence dans la fenêtre de données actuelle.",
    "sustainedHighWaitTimes": "Temps d'attente élevés soutenus",
    "sustainedHighDesc": "Établissements avec au moins 50 lectures, classés par temps d'attente moyen.",
    "volatility": "Volatilité",
    "volatilityDesc": "Un écart-type élevé signifie que l'établissement varie plus que ses pairs.",
    "facility": "Établissement",
    "type": "Type",
    "waitTime": "Temps d'attente",
    "estimatedLengthOfStay": "Durée de séjour estimée",
    "observed": "Observé",
    "currentWait": "Temps d'attente actuel",
    "averageWait": "Temps d'attente moyen",
    "deltaFromAverage": "Écart par rapport à la moyenne",
    "zScore": "Score Z",
    "medianWait": "Temps d'attente médian",
    "maxWait": "Temps d'attente maximal",
    "standardDeviation": "Écart-type",
    "readings": "Lectures",
    "locationsWithoutReadingsTitle": "Emplacements sans lectures de temps d'attente",
    "locationsWithoutReadingsDesc": "Utile pour les vérifications de qualité des données et les audits de l'état du produit.",
    "showWaitTimes": "Afficher les temps d'attente",
    "fallbackText": "Texte de temps d'attente par défaut",
    "everyLocationHasReadings": "Chaque emplacement suivi a au moins une lecture de temps d'attente.",
    "databaseUnavailable": "Base de données indisponible",
    "dbErrorTitle": "La page analytique n'a pas pu lire Postgres.",
    "dbErrorDesc": "Vérifiez DATABASE_URL ou le fichier d'environnement utilisé pour démarrer Next, puis rafraîchissez cette page.",
    "backToFacilities": "Retour aux établissements",
    "shellTitle": "La page analytique se charge sans données.",
    "shellDesc": "La route a été rendue avec succès sans requêtes de base de données. Les panneaux de données sont temporairement désactivés pendant la vérification de l'accès en production.",
    "method1Title": "Temps d'attente médian et P90 d'abord",
    "method1Body": "Le temps d'attente moyen est utile, mais le médian et le P90 sont meilleurs pour la pression publique et les décisions de risque de queue.",
    "method2Title": "Référence par site",
    "method2Body": "Chaque établissement a sa propre plage normale, les alertes comparent donc les temps d'attente actuels à l'historique propre du site.",
    "method3Title": "Séparer les lacunes structurelles",
    "method3Body": "Les emplacements avec l'affichage du temps d'attente masqué doivent être présentés comme l'état du produit, sans les confondre avec de la télémétrie défaillante.",
    "yes": "oui",
    "no": "non"
  },
  "charts": {
    "currentAccessPressure": "Pression d'accès actuelle",
    "currentAccessPressureDesc": "Temps d'attente actuels les plus longs, colorés par seuil de gravité.",
    "edVsUpccTrend": "Tendance urgences vs CSUP",
    "edVsUpccTrendDesc": "Temps d'attente médian et P90 par type de soins, présentés séparément pour un risque de queue plus clair.",
    "sustainedFacilityPressure": "Pression soutenue des établissements",
    "sustainedFacilityPressureDesc": "Établissements classés par temps d'attente P90. Le médian montre la pression typique ; le P90 montre la pression des mauvais jours.",
    "currentVsMedian": "Temps d'attente actuel vs médiane habituelle",
    "currentVsMedianDesc": "Le point neutre est le temps d'attente médian du site ; le point coloré est le temps d'attente actuel. La ligne montre l'écart.",
    "tailRiskMap": "Carte de risque de queue",
    "tailRiskMapDesc": "Temps d'attente médian vs P90 par établissement. Les sites en haut à droite sont généralement lents et douloureux en queue.",
    "waitDistribution": "Distribution des temps d'attente",
    "waitDistributionDesc": "Nombre de lectures par tranche de temps d'attente, utile pour les vérifications de pression de longue queue.",
    "facilityHourPattern": "Schéma heure-établissement",
    "facilityHourPatternDesc": "Établissements à haute pression soutenue par heure locale. Plus sombre signifie un temps d'attente moyen plus élevé.",
    "coverageVolume": "Volume de couverture",
    "coverageVolumeDesc": "Lectures par établissement. Les établissements à faible volume doivent être interprétés avec prudence.",
    "under1h": "moins de 1h",
    "over2h": "2h+",
    "over4h": "4h+",
    "over6h": "6h+",
    "edMedianWait": "Attente médiane urgences",
    "edP90Wait": "Attente P90 urgences",
    "upccMedianWait": "Attente médiane CSUP",
    "upccP90Wait": "Attente P90 CSUP",
    "currentWaitTime": "Temps d'attente actuel"
  },
  "metadata": {
    "siteTitle": "EDWT · Temps d'attente aux urgences du Lower Mainland",
    "siteDescription": "Temps d'attente en direct pour les urgences et les centres de soins urgents du Lower Mainland, C.-B. Mis à jour chaque minute depuis le flux edwaittimes.ca.",
    "ogTitle": "EDWT · Temps d'attente en direct aux urgences",
    "analyticsTitle": "Analytique des temps d'attente",
    "analyticsDescription": "Analytique systémique des temps d'attente aux urgences et CSUP du Lower Mainland — tendances, cartes thermiques, risque de queue et couverture des établissements suivis.",
    "mapTitle": "Carte des établissements",
    "mapDescription": "Carte interactive des urgences et centres de soins urgents du Lower Mainland, C.-B. avec les temps d'attente en direct et les itinéraires.",
    "jsonLdWebsiteDesc": "Temps d'attente en direct pour les urgences et les centres de soins urgents du Lower Mainland, C.-B. Mis à jour toutes les 60 secondes à partir des données officielles des autorités sanitaires de la C.-B.",
    "jsonLdAppDesc": "Suivi gratuit en temps réel des temps d'attente aux urgences et centres de soins urgents du Lower Mainland, Colombie-Britannique. Affiche les temps d'attente en direct, le nombre de médecins et les volumes de patients pour plus de 12 hôpitaux et CSUP. Données provenant du flux officiel edwaittimes.ca toutes les 60 secondes.",
    "jsonLdFeatureList": "Temps d'attente en direct, Tri par distance GPS, Carte interactive, Analytique historique, Application web progressive, Rafraîchissement toutes les 60 secondes"
  }
}
```

- [ ] **Step 2: Verify JSON is valid**

```bash
node -e "JSON.parse(require('fs').readFileSync('messages/fr.json','utf8')); console.log('valid')"
```

Expected: `valid`

- [ ] **Step 3: Commit**

```bash
git add messages/fr.json
git commit -m "feat(i18n): add French translation file"
```

---

### Task 4: Wire next-intl into Next.js (request config, middleware, next.config)

**Files:**
- Create: `src/i18n/request.ts`
- Create: `src/middleware.ts`
- Modify: `next.config.ts`

- [ ] **Step 1: Create `src/i18n/request.ts`**

```ts
import { getRequestConfig } from "next-intl/server";
import { cookies, headers } from "next/headers";
import { defaultLocale, locales, type Locale } from "./config";

function isLocale(value: string): value is Locale {
  return (locales as readonly string[]).includes(value);
}

function negotiateLocale(acceptLanguage: string | null): Locale {
  if (!acceptLanguage) return defaultLocale;
  const preferred = acceptLanguage
    .split(",")
    .map((part) => part.split(";")[0].trim().split("-")[0].toLowerCase());
  for (const lang of preferred) {
    if (isLocale(lang)) return lang;
  }
  return defaultLocale;
}

export default getRequestConfig(async () => {
  const cookieStore = await cookies();
  const headerStore = await headers();

  const cookieLocale = cookieStore.get("NEXT_LOCALE")?.value;
  const locale: Locale =
    cookieLocale && isLocale(cookieLocale)
      ? cookieLocale
      : negotiateLocale(headerStore.get("accept-language"));

  return {
    locale,
    messages: (await import(`../../messages/${locale}.json`)).default,
  };
});
```

- [ ] **Step 2: Create `src/middleware.ts`**

```ts
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";

export function middleware(request: NextRequest) {
  const response = NextResponse.next();
  const locale = request.cookies.get("NEXT_LOCALE")?.value;
  if (locale) {
    response.headers.set("x-next-intl-locale", locale);
  }
  return response;
}

export const config = {
  matcher: ["/((?!_next|monitoring|api|favicon.ico|health-authorities|.*\\..*).*)"],
};
```

The matcher excludes static assets, the Sentry tunnel route (`/monitoring`), API routes, and favicon.

- [ ] **Step 3: Update `next.config.ts`**

Replace the current file content. The key change is wrapping with `createNextIntlPlugin`:

```ts
import { withSentryConfig } from "@sentry/nextjs";
import createNextIntlPlugin from "next-intl/plugin";
import type { NextConfig } from "next";

const withNextIntl = createNextIntlPlugin("./src/i18n/request.ts");

const nextConfig: NextConfig = {
  allowedDevOrigins: ["10.0.0.165"],
  async headers() {
    return [
      {
        source: "/llms.txt",
        headers: [
          { key: "Content-Type", value: "text/markdown; charset=utf-8" },
          { key: "Cache-Control", value: "public, max-age=3600" },
        ],
      },
      {
        source: "/llms-full.txt",
        headers: [
          { key: "Content-Type", value: "text/markdown; charset=utf-8" },
          { key: "Cache-Control", value: "public, max-age=3600" },
        ],
      },
    ];
  },
};

export default withSentryConfig(withNextIntl(nextConfig), {
  org: "nnfewl",
  project: "edwt-next",
  silent: !process.env.CI,
  widenClientFileUpload: true,
  tunnelRoute: "/monitoring",
  webpack: {
    automaticVercelMonitors: true,
    treeshake: {
      removeDebugLogging: true,
    },
  },
});
```

- [ ] **Step 4: Verify build compiles**

```bash
pnpm build
```

Expected: Build succeeds. If there are type errors from next-intl, fix them before continuing.

- [ ] **Step 5: Commit**

```bash
git add src/i18n/request.ts src/middleware.ts next.config.ts
git commit -m "feat(i18n): wire next-intl request config, middleware, and plugin"
```

---

### Task 5: Update layout.tsx — dynamic lang, provider, translated metadata

**Files:**
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Update `layout.tsx`**

Replace the full file with:

```tsx
import type { Metadata } from "next";
import { Geist_Mono, Manrope } from "next/font/google";
import { NextIntlClientProvider } from "next-intl";
import { getLocale, getMessages, getTranslations } from "next-intl/server";
import { Analytics } from "@vercel/analytics/next";
import { AppTopBar } from "./app-topbar";
import "./globals.css";

const manrope = Manrope({
  variable: "--font-manrope",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const siteUrl = "https://edwt.ca";
const siteName = "EDWT";

export async function generateMetadata(): Promise<Metadata> {
  const t = await getTranslations("metadata");
  const locale = await getLocale();
  const ogLocale = locale === "fr" ? "fr_CA" : "en_CA";

  return {
    title: {
      default: t("siteTitle"),
      template: "%s · EDWT",
    },
    description: t("siteDescription"),
    metadataBase: new URL(siteUrl),
    alternates: { canonical: "/" },
    keywords: [
      "emergency wait times",
      "ED wait times",
      "Lower Mainland",
      "BC emergency",
      "UPCC wait times",
      "urgent care",
      "Vancouver ER",
      "hospital wait times",
      "edwaittimes",
    ],
    openGraph: {
      type: "website",
      locale: ogLocale,
      url: siteUrl,
      siteName,
      title: t("ogTitle"),
      description: t("siteDescription"),
    },
    twitter: {
      card: "summary_large_image",
      title: t("ogTitle"),
      description: t("siteDescription"),
    },
    robots: {
      index: true,
      follow: true,
      googleBot: { index: true, follow: true },
    },
  };
}

export default async function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  const locale = await getLocale();
  const messages = await getMessages();

  return (
    <html
      lang={locale}
      className={`${manrope.variable} ${geistMono.variable} h-full antialiased`}
    >
      <head>
        <link rel="preconnect" href="https://basemaps.cartocdn.com" crossOrigin="anonymous" />
        <link rel="preconnect" href="https://tiles.basemaps.cartocdn.com" crossOrigin="anonymous" />
        <link
          rel="alternate"
          type="text/markdown"
          href="/llms.txt"
          title="LLM-readable site description"
        />
      </head>
      <body className="min-h-full flex flex-col">
        <NextIntlClientProvider messages={messages}>
          <AppTopBar />
          {children}
        </NextIntlClientProvider>
        <Analytics />
      </body>
    </html>
  );
}
```

Key changes:
- `export const metadata` → `export async function generateMetadata()` (uses `getTranslations`)
- `<html lang="en">` → `<html lang={locale}>` (dynamic from cookie)
- Children wrapped in `<NextIntlClientProvider>`
- OG locale switches between `en_CA` and `fr_CA`

- [ ] **Step 2: Verify dev server starts**

```bash
pnpm dev
```

Visit `http://localhost:3000` — page should render identically (still English, no cookie set).

- [ ] **Step 3: Commit**

```bash
git add src/app/layout.tsx
git commit -m "feat(i18n): dynamic lang, NextIntlClientProvider, translated metadata in layout"
```

---

### Task 6: Add locale toggle to AppTopBar and translate nav labels

**Files:**
- Modify: `src/app/app-topbar.tsx`

- [ ] **Step 1: Update `app-topbar.tsx`**

Replace the full file with:

```tsx
"use client";

import Link from "next/link";
import { usePathname, useRouter } from "next/navigation";
import { useLayoutEffect, useRef, useState } from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faBars, faChartLine, faHospital, faList, faMapLocationDot } from "@fortawesome/free-solid-svg-icons";
import { useLocale, useTranslations } from "next-intl";

type AppTopBarActive = "list" | "map" | "analytics";

type NavIndicator = {
  left: number;
  width: number;
  ready: boolean;
  animate: boolean;
};

const NAV_ITEMS = [
  { id: "list", href: "/", labelKey: "facilities", icon: faList },
  { id: "map", href: "/map", labelKey: "map", icon: faMapLocationDot },
  { id: "analytics", href: "/analytics", labelKey: "analytics", icon: faChartLine },
] satisfies Array<{ id: AppTopBarActive; href: string; labelKey: string; icon: typeof faList }>;

function activeFromPathname(pathname: string): AppTopBarActive {
  if (pathname.startsWith("/map")) return "map";
  if (pathname.startsWith("/analytics") || pathname.startsWith("/admin")) return "analytics";
  return "list";
}

function LocaleToggle() {
  const locale = useLocale();
  const router = useRouter();
  const nextLocale = locale === "en" ? "fr" : "en";

  const switchLocale = () => {
    document.cookie = `NEXT_LOCALE=${nextLocale};path=/;max-age=31536000;samesite=lax`;
    router.refresh();
  };

  return (
    <button
      className="app-locale-toggle"
      type="button"
      onClick={switchLocale}
      aria-label={nextLocale === "fr" ? "Passer au français" : "Switch to English"}
    >
      {nextLocale.toUpperCase()}
    </button>
  );
}

export function AppTopBar() {
  const t = useTranslations("nav");
  const pathname = usePathname();
  const active = activeFromPathname(pathname);
  const menuRef = useRef<HTMLDetailsElement>(null);
  const navRef = useRef<HTMLElement>(null);
  const linkRefs = useRef<Record<AppTopBarActive, HTMLAnchorElement | null>>({
    list: null,
    map: null,
    analytics: null,
  });
  const [indicator, setIndicator] = useState<NavIndicator>({
    left: 0,
    width: 0,
    ready: false,
    animate: false,
  });
  const closeMenu = () => menuRef.current?.removeAttribute("open");

  useLayoutEffect(() => {
    const measure = (tab: AppTopBarActive) => {
      const link = linkRefs.current[tab];
      if (!link) return null;
      return { left: link.offsetLeft, width: link.offsetWidth };
    };

    const current = measure(active);
    if (!current) return undefined;

    let firstFrame = 0;

    setIndicator((state) => {
      if (state.ready) return { ...current, ready: true, animate: true };
      firstFrame = window.requestAnimationFrame(() => {
        setIndicator((state) => ({ ...state, animate: true }));
      });
      return { ...current, ready: true, animate: false };
    });

    const resizeObserver = new ResizeObserver(() => {
      const next = measure(active);
      if (next) setIndicator({ ...next, ready: true, animate: false });
    });

    if (navRef.current) resizeObserver.observe(navRef.current);

    return () => {
      window.cancelAnimationFrame(firstFrame);
      resizeObserver.disconnect();
    };
  }, [active]);

  return (
    <header className="app-topbar">
      <div className="app-topbar-inner">
        <Link href="/" className="app-wordmark">
          <span className="app-wordmark-mark" aria-hidden="true">
            <FontAwesomeIcon icon={faHospital} />
          </span>
          <span>EDWT</span>
        </Link>

        <nav className={`app-nav-tabs${indicator.ready ? " is-ready" : ""}`} aria-label="Primary" ref={navRef}>
          <span
            className={`app-nav-indicator${indicator.ready ? " is-ready" : ""}${indicator.animate ? " is-animated" : ""}`}
            style={{ width: indicator.width, transform: `translateX(${indicator.left}px)` }}
            aria-hidden="true"
          />
          {NAV_ITEMS.map((item) => (
            <Link
              href={item.href}
              key={item.id}
              className={active === item.id ? "active" : ""}
              aria-current={active === item.id ? "page" : undefined}
              ref={(node) => {
                linkRefs.current[item.id] = node;
              }}
            >
              <FontAwesomeIcon icon={item.icon} aria-hidden="true" /> {t(item.labelKey)}
            </Link>
          ))}
        </nav>

        <details className="app-mobile-menu" ref={menuRef}>
          <summary aria-label="Open page menu">
            <FontAwesomeIcon icon={faBars} aria-hidden="true" />
          </summary>
          <div className="app-mobile-menu-panel" role="menu">
            <Link href="/" className={active === "list" ? "active" : ""} onClick={closeMenu}>
              <FontAwesomeIcon icon={faList} aria-hidden="true" /> {t("facilities")}
            </Link>
            <Link href="/map" className={active === "map" ? "active" : ""} onClick={closeMenu}>
              <FontAwesomeIcon icon={faMapLocationDot} aria-hidden="true" /> {t("map")}
            </Link>
            <Link href="/analytics" className={active === "analytics" ? "active" : ""} onClick={closeMenu}>
              <FontAwesomeIcon icon={faChartLine} aria-hidden="true" /> {t("analytics")}
            </Link>
          </div>
        </details>

        <div className="app-topbar-spacer" />
        <LocaleToggle />
        <div className="app-live-pill">
          <span aria-hidden="true" />
          {t("liveWaitTimes")}
        </div>
      </div>
    </header>
  );
}
```

Key changes:
- `NAV_ITEMS` uses `labelKey` instead of `label`, resolved via `t(item.labelKey)`
- Mobile menu links use `t()` for labels
- "Live wait times" pill text uses `t("liveWaitTimes")`
- `LocaleToggle` component added between spacer and live pill
- `useLocale` and `useTranslations` imported from `next-intl`

- [ ] **Step 2: Add toggle styling to `globals.css`**

Add at the end of `src/app/globals.css`:

```css
.app-locale-toggle {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 28px;
  padding: 0 10px;
  border: 1px solid var(--line);
  border-radius: 6px;
  background: transparent;
  color: var(--ink);
  font-size: 12px;
  font-weight: 700;
  letter-spacing: 0.04em;
  cursor: pointer;
  transition: background 150ms, border-color 150ms;
  flex-shrink: 0;
}
.app-locale-toggle:hover {
  background: var(--line);
  border-color: var(--muted);
}
```

- [ ] **Step 3: Verify in browser**

```bash
pnpm dev
```

Visit `http://localhost:3000`. You should see:
- Nav labels still say "Facilities", "Map", "Analytics" (English, default)
- A `FR` button appears in the topbar
- Clicking `FR` reloads the page with French nav labels: "Établissements", "Carte", "Analytique"
- Clicking `EN` switches back

- [ ] **Step 4: Commit**

```bash
git add src/app/app-topbar.tsx src/app/globals.css
git commit -m "feat(i18n): translate topbar nav labels and add locale toggle"
```

---

### Task 7: Update data.ts — severity/status functions return translation keys

**Files:**
- Modify: `src/app/data.ts`

- [ ] **Step 1: Update `severityLabel` and `facilityWaitStatusLabel`**

Change these two functions in `src/app/data.ts`:

Replace:
```ts
export function severityLabel(min: number | null): string {
  if (min == null) return "No data";
  if (min <= 60) return "Short wait";
  if (min <= 180) return "Moderate wait";
  return "Long wait";
}

export function facilityWaitStatusLabel(facility: Pick<Facility, "open" | "waitMin">): string {
  if (!facility.open) return "Closed";
  return severityLabel(facility.waitMin);
}
```

With:
```ts
export function severityLabelKey(min: number | null): string {
  if (min == null) return "noData";
  if (min <= 60) return "shortWait";
  if (min <= 180) return "moderateWait";
  return "longWait";
}

export function facilityWaitStatusLabelKey(facility: Pick<Facility, "open" | "waitMin">): string {
  if (!facility.open) return "closed";
  return severityLabelKey(facility.waitMin);
}
```

These now return keys that map to the `common` namespace in the translation files.

- [ ] **Step 2: Commit**

```bash
git add src/app/data.ts
git commit -m "refactor(i18n): severity/status functions return translation keys instead of English strings"
```

---

### Task 8: Translate page-client.tsx (facilities list page)

**Files:**
- Modify: `src/app/page-client.tsx`

This is the largest file (~1317 lines). The changes are mechanical: add `useTranslations` and replace every hardcoded string with `t()` calls.

- [ ] **Step 1: Add imports and hook**

At the top of the file, add to the existing imports:

```ts
import { useTranslations } from "next-intl";
```

Update the import from `./data` to use the renamed functions:

```ts
import {
  type Facility,
  type HistoryPoint,
  facilityWaitStatusLabelKey,
  severityFor,
  severityLabelKey,
} from "./data";
```

- [ ] **Step 2: Update FacilityCard component**

In `FacilityCard`, add at the top of the function body:

```ts
const t = useTranslations("facilities");
const tc = useTranslations("common");
```

Then replace hardcoded strings:
- `{isEm ? "Emergency" : "UPCC"}` → `{isEm ? tc("emergency") : tc("upcc")}`
- `{f.inWaitingRoom} waiting` → `{t("waiting", { count: f.inWaitingRoom })}`
- `{f.physiciansOnDuty} on duty` → `{t("onDuty", { count: f.physiciansOnDuty })}`
- `title="Directions"` → `title={t("directions")}`
- `<span className="action-label">Directions</span>` → `<span className="action-label">{t("directions")}</span>`
- `title="Call"` → `title={t("call")}`
- `<span className="action-label">Call</span>` → `<span className="action-label">{t("call")}</span>`
- `title="Details"` → `title={t("details")}`
- `<span className="action-label">Details</span>` → `<span className="action-label">{t("details")}</span>`
- `const sevLabel = facilityWaitStatusLabel(f)` → `const sevLabel = tc(facilityWaitStatusLabelKey(f))`
- `<strong>No data</strong>` → `<strong>{tc("noData")}</strong>`
- `<span>No wait posted</span>` → `<span>{t("noWaitPosted")}</span>`
- `<strong>Closed</strong>` → `<strong>{tc("closed")}</strong>`
- `Updated {f.lastUpdated}` → `{tc("updated", { time: f.lastUpdated })}`

- [ ] **Step 3: Update DetailsDrawer component**

In `DetailsDrawer`, add at the top:

```ts
const t = useTranslations("facilities");
const tc = useTranslations("common");
```

Replace all hardcoded strings using the same pattern as FacilityCard, plus:
- `"What to expect"` → `{t("whatToExpect")}`
- `"Address"` → `{t("address")}`
- `"Hours"` → `{t("hours")}`
- `"Website"` → `{t("website")}`
- The long prose in "What to expect" → `{t("expectOpen")}`, `{t("expectNoWait")}`, `{t("expectClosed")}`
- `{facilityWaitStatusLabel(f)}` → `{tc(facilityWaitStatusLabelKey(f))}`
- `"Visit {f.name} website"` → `{t("visitWebsite", { name: f.name })}`
- `"Call {f.phone}"` → `{t("callPhone", { phone: f.phone })}`
- `aria-label="Close"` → `aria-label={tc("close")}`

- [ ] **Step 4: Update ERNowPageClient main body**

At the top of `ERNowPageClient`, add:

```ts
const t = useTranslations("facilities");
const tc = useTranslations("common");
```

Update the `FILTERS` and `SORTS` arrays. Since they use `as const` and are module-level, convert them to use translation keys and resolve in JSX:

Replace the `FILTERS` constant:

```ts
const FILTER_KEYS = [
  { id: "all", labelKey: "allFacilities" },
  { id: "emergency", labelKey: "emergency" },
  { id: "upcc", labelKey: "walkInUpcc" },
  { id: "pediatric", labelKey: "pediatric" },
  { id: "open", labelKey: "openNow" },
] as const;
```

Replace the `SORTS` constant:

```ts
const SORT_KEYS = [
  { id: "wait", labelKey: "shortestWait", shortLabelKey: "shortestWait", icon: "clock", descKey: "sortWaitDesc" },
  { id: "distance", labelKey: "closestFirst", shortLabelKey: "closestFirst", icon: "pin", descKey: "sortDistDesc" },
  { id: "name", labelKey: "nameAZ", shortLabelKey: "nameAZ", icon: "list", descKey: "sortNameDesc" },
] as const;
```

Then in the JSX, resolve all labels via `t(f.labelKey)`, `t(s.labelKey)`, etc.

Replace all remaining hardcoded strings in the main body:
- Hero `<h1>` → `{t.rich("heroTitle", { em: (chunks) => <em>{chunks}</em> })}`
- Hero `<p>` → `{t("heroSub")}`
- Location labels → `{t("preciseLocation")}`, `{t("approximateLocation")}`, etc.
- `heroDate` / `heroTime` → pass `locale` to `toLocaleDateString` / `toLocaleTimeString`: use `useLocale()` from next-intl
- Emergency banner → `{t("emergencyBannerStrong")}` and `{t.rich("emergencyBannerBody", { healthLink: ... })}`
- Stats section → `{t("statShortestWait")}`, `{t("statClosestOpen")}`, etc.
- Recommended pick → `{t("recommendedForYou")}`, `{t("shortestReportedWait", { wait: ... })}`, etc.
- Empty state → `{t("noMatch")}`, `{t("showAll")}`
- Advice cards → `{t("advice01Title")}`, `{t("advice01Body")}`, etc.
- Footer → `{t("footerTagline")}`, `{t("serviceStatus")}`, `{t("footnote")}`
- Sort sheet → `{t("sortFacilities")}`, `{t("chooseOrder")}`, `{t("sort")}`, `{t("sortedBy")}`

- [ ] **Step 5: Verify dev server**

```bash
pnpm dev
```

Visit `http://localhost:3000`. Toggle to French and verify:
- All filter pills show French labels
- Sort options show French
- Hero text is in French
- Facility cards show translated status labels
- Details drawer shows French
- Footer shows French

- [ ] **Step 6: Commit**

```bash
git add src/app/page-client.tsx
git commit -m "feat(i18n): translate facilities list page (page-client.tsx)"
```

---

### Task 9: Translate page.tsx (JSON-LD and server metadata)

**Files:**
- Modify: `src/app/page.tsx`

- [ ] **Step 1: Update `page.tsx`**

Make JSON-LD descriptions translatable:

```tsx
import type { Metadata } from "next";
import { getTranslations } from "next-intl/server";
import { AutoRefresh } from "./auto-refresh";
import { getPublicFacilities } from "./facilities-db";
import { getApproximateLocationOrigin } from "./location-origin";
import { ERNowPageClient } from "./page-client";

export const metadata: Metadata = {
  alternates: { canonical: "/" },
};

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default async function ERNowPage() {
  const [initialOrigin, facilities, t] = await Promise.all([
    getApproximateLocationOrigin(),
    getPublicFacilities(),
    getTranslations("metadata"),
  ]);

  const jsonLd = [
    {
      "@context": "https://schema.org",
      "@type": "WebSite",
      name: "EDWT",
      url: "https://edwt.ca",
      description: t("jsonLdWebsiteDesc"),
      potentialAction: {
        "@type": "SearchAction",
        target: "https://edwt.ca/?q={search_term_string}",
        "query-input": "required name=search_term_string",
      },
    },
    {
      "@context": "https://schema.org",
      "@type": "WebApplication",
      name: "EDWT",
      url: "https://edwt.ca",
      applicationCategory: "HealthApplication",
      operatingSystem: "Any",
      description: t("jsonLdAppDesc"),
      featureList: t("jsonLdFeatureList"),
      areaServed: {
        "@type": "GeoCircle",
        geoMidpoint: { "@type": "GeoCoordinates", latitude: 49.2, longitude: -122.9 },
        geoRadius: "80000",
      },
      provider: {
        "@type": "Organization",
        name: "EDWT",
        url: "https://edwt.ca",
      },
    },
  ];

  return (
    <>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
      />
      <AutoRefresh />
      <ERNowPageClient facilities={facilities} initialOrigin={initialOrigin} />
    </>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/app/page.tsx
git commit -m "feat(i18n): translate JSON-LD descriptions on home page"
```

---

### Task 10: Translate map page

**Files:**
- Modify: `src/app/map/page.tsx`
- Modify: `src/app/map/map-client.tsx`

- [ ] **Step 1: Update `map/page.tsx` metadata**

```tsx
import type { Metadata } from "next";
import { getTranslations } from "next-intl/server";
import { AutoRefresh } from "../auto-refresh";
import { getPublicFacilities } from "../facilities-db";
import { getApproximateLocationOrigin } from "../location-origin";
import { MapClientLazy } from "./map-client-lazy";

export async function generateMetadata(): Promise<Metadata> {
  const t = await getTranslations("metadata");
  return {
    title: t("mapTitle"),
    description: t("mapDescription"),
    alternates: { canonical: "/map" },
  };
}

export const dynamic = "force-dynamic";
export const revalidate = 0;

type MapPageProps = {
  searchParams?: Promise<Record<string, string | string[] | undefined>>;
};

function firstParam(value: string | string[] | undefined): string | null {
  if (Array.isArray(value)) return value[0] ?? null;
  return value ?? null;
}

export default async function MapPage({ searchParams }: MapPageProps) {
  const params = (await searchParams) ?? {};
  const [initialOrigin, facilities] = await Promise.all([
    getApproximateLocationOrigin(),
    getPublicFacilities(),
  ]);

  return (
    <>
      <AutoRefresh />
      <MapClientLazy
        facilities={facilities}
        initialOrigin={initialOrigin}
        initialFacilityId={firstParam(params.facility)}
        routeRequested={firstParam(params.route) === "1"}
      />
    </>
  );
}
```

- [ ] **Step 2: Update `map/map-client.tsx`**

Add at the top of imports:

```ts
import { useTranslations } from "next-intl";
```

Update the import from `../data`:

```ts
import { type Facility, facilityWaitStatusLabelKey, severityFor } from "../data";
```

At the top of the `MapClient` component function body, add:

```ts
const t = useTranslations("map");
const tc = useTranslations("common");
```

Then replace all hardcoded strings:
- `"No data"` → `tc("noData")`
- `"Shortest open wait"` → `{t("shortestOpenWait")}`
- `"Highest pressure"` → `{t("highestPressure")}`
- `"No posted waits"` → `{t("noPostedWaits")}`
- `"No open facilities"` → `{t("noOpenFacilities")}`
- `{selected.type}` pill → `{tc(selected.type === "Emergency" ? "emergency" : "upcc")}`
- `"Open"` status pill → `{tc("open")}`
- `{facilityWaitStatusLabel(selected)}` → `{tc(facilityWaitStatusLabelKey(selected))}`
- `"Facility is closed"` → `{t("facilityIsClosed")}`
- `"Closed"` → `{tc("closed")}`
- `"Distance"` → `{t("distance")}`
- `"Waiting"` → `{t("waiting")}`
- `"On duty"` → `{t("onDuty")}`
- `"This site is currently in the top pressure group."` → `{t("topPressureNote")}`
- `"Routing..."` → `{t("routing")}`
- `"Directions"` → `{t("directions")}`
- `"Center map"` → `{t("centerMap")}`
- `"Call"` → `{tc("close")}` — wait, it should be the call label from common. Actually "Call" in the map context: keep as `{t("directions")}` for directions button and for call just render the phone link text.
- Route note text → `{t("routeFrom", { origin: route.originLabel, distance: route.distanceKm.toFixed(1), duration: Math.round(route.durationMin).toString() })}`
- `"Precise location is required..."` → `{t("locationRequired")}`
- `"Finding your location"` / `"Show your location"` → These are set via DOM manipulation in `useEffect` — pass the translated strings via refs or compute them outside the effect. The simplest approach: define `const locationBtnTitle = locating ? t("findingLocation") : t("showYourLocation");` and set it in the `useEffect` that updates the button.
- Map sidebar copy → `{t("reporting", { count: facilitiesWithDistance.length })}`, `{t("nearbyFacilities")}`, `{t("mapSidebarDesc")}`
- `"Map unavailable"` → `{t("mapUnavailable")}`
- `"Your location"` aria-label → `{t("yourLocation")}`

- [ ] **Step 3: Verify in browser**

```bash
pnpm dev
```

Visit `http://localhost:3000/map`, toggle to French, verify all sidebar text, selected card, and stat labels render in French.

- [ ] **Step 4: Commit**

```bash
git add src/app/map/page.tsx src/app/map/map-client.tsx
git commit -m "feat(i18n): translate map page"
```

---

### Task 11: Translate analytics page (server component)

**Files:**
- Modify: `src/app/analytics/page.tsx`

- [ ] **Step 1: Update analytics page**

This is a server component, so use `getTranslations`:

At the top, add:

```ts
import { getLocale, getTranslations } from "next-intl/server";
```

Replace the static `export const metadata` with `generateMetadata`:

```ts
export async function generateMetadata(): Promise<Metadata> {
  const t = await getTranslations("metadata");
  return {
    title: t("analyticsTitle"),
    description: t("analyticsDescription"),
    alternates: { canonical: "/analytics" },
  };
}
```

In `AnalyticsPage`, add at the start of the function:

```ts
const [t, locale] = await Promise.all([
  getTranslations("analytics"),
  getLocale(),
]);
```

Update `localFormatter` to use the locale:

```ts
const localFormatter = new Intl.DateTimeFormat(locale === "fr" ? "fr-CA" : "en-CA", {
  timeZone: "America/Vancouver",
  month: "short",
  day: "2-digit",
  hour: "2-digit",
  minute: "2-digit",
  hour12: false,
});
```

Note: `localFormatter` is currently module-level. It needs to move inside the component or become a function that takes locale, since it now depends on runtime locale. The simplest: make `fmtDate` accept a formatter parameter, or move the formatter creation inside `AnalyticsPage` and pass it down.

Since the helper functions (`fmtNumber`, `fmtWait`, `fmtDate`, etc.) are module-level, and `fmtNumber` uses `"en-US"` locale, update `fmtNumber` to accept an optional locale parameter. Alternatively, since this is a server component rendered once per request, the simplest approach is to move the formatter into the page function and pass it through a closure. Keep the helpers module-level but make the formatter a parameter:

```ts
function fmtDate(value: Date | string | null | undefined, formatter: Intl.DateTimeFormat) {
  if (!value) return "n/a";
  return formatter.format(new Date(value));
}
```

Then replace all `fmtDate(value)` calls with `fmtDate(value, localFormatter)` where `localFormatter` is created inside the page function.

For `fmtNumber`, change:

```ts
function fmtNumber(value: MaybeNumber | undefined, digits = 0, locale = "en-US") {
  if (value === null || value === undefined || Number.isNaN(value)) return "n/a";
  return new Intl.NumberFormat(locale, { maximumFractionDigits: digits }).format(value);
}
```

Then replace every hardcoded English string in the JSX with `t()` calls using the `analytics` namespace keys from `en.json`. This is mechanical — every heading, table header, metric label, eyebrow, etc. gets a `t("keyName")` call. There are ~60 strings.

Key replacements:
- `"Live wait-time analytics"` → `{t("liveAnalytics")}`
- `"Wait-time analytics"` → `{t("heroTitle")}`
- All `<MetricCard label=...>` → `label={t("facilitiesTracked")}`, etc.
- All `<DataTable title=...>` → `title={t("currentWaitPressure")}`, etc.
- Table `<th>` headers → `{t("facility")}`, `{t("type")}`, `{t("waitTime")}`, etc.
- Methodology section → `{t("method1Title")}`, `{t("method1Body")}`, etc.
- `"yes"` / `"no"` in the no-readings table → `{t("yes")}` / `{t("no")}`
- Error panels → `{t("databaseUnavailable")}`, `{t("dbErrorTitle")}`, `{t("backToFacilities")}`

- [ ] **Step 2: Verify in browser**

```bash
pnpm dev
```

Visit `http://localhost:3000/analytics`, toggle to French, verify all headings, table headers, and metric labels render in French.

- [ ] **Step 3: Commit**

```bash
git add src/app/analytics/page.tsx
git commit -m "feat(i18n): translate analytics server page"
```

---

### Task 12: Translate analytics-charts.tsx (client component)

**Files:**
- Modify: `src/app/analytics/analytics-charts.tsx`

- [ ] **Step 1: Add translations hook**

Add to imports:

```ts
import { useTranslations } from "next-intl";
```

At the top of `AnalyticsCharts` function body:

```ts
const t = useTranslations("charts");
```

Replace all `ChartShell` `title` and `eyebrow` props:

- `title="Current access pressure"` → `title={t("currentAccessPressure")}`
- `eyebrow="Longest current wait times..."` → `eyebrow={t("currentAccessPressureDesc")}`
- `title="ED vs UPCC trend"` → `title={t("edVsUpccTrend")}`
- `eyebrow="Median wait time..."` → `eyebrow={t("edVsUpccTrendDesc")}`
- `title="Sustained facility pressure"` → `title={t("sustainedFacilityPressure")}`
- `title="Current wait time vs usual median"` → `title={t("currentVsMedian")}`
- `title="Tail-risk map"` → `title={t("tailRiskMap")}`
- `title="Wait-time distribution"` → `title={t("waitDistribution")}`
- `title="Facility-hour pattern"` → `title={t("facilityHourPattern")}`
- `title="Coverage volume"` → `title={t("coverageVolume")}`

Replace legend text:
- `"under 1h"` → `{t("under1h")}`
- `"2h+"` → `{t("over2h")}`
- `"4h+"` → `{t("over4h")}`
- `"6h+"` → `{t("over6h")}`
- `"ED median wait time"` → `{t("edMedianWait")}`
- `"ED P90 wait time"` → `{t("edP90Wait")}`
- `"UPCC median wait time"` → `{t("upccMedianWait")}`
- `"UPCC P90 wait time"` → `{t("upccP90Wait")}`

For chart.js dataset labels and tooltip callbacks that contain English text (like `"Current wait time"` in the bar chart config), replace with `t("currentWaitTime")`. Since `t()` is called at render time and chart configs are built in `useMemo`, pass the translated strings into the memo dependencies.

- [ ] **Step 2: Verify in browser**

```bash
pnpm dev
```

Visit `http://localhost:3000/analytics`, toggle to French. Chart titles, eyebrows, and legend labels should be in French.

- [ ] **Step 3: Commit**

```bash
git add src/app/analytics/analytics-charts.tsx
git commit -m "feat(i18n): translate analytics chart titles and legends"
```

---

### Task 13: Final build verification

- [ ] **Step 1: Run production build**

```bash
pnpm build
```

Expected: Build succeeds with no errors.

- [ ] **Step 2: Run lint**

```bash
pnpm lint
```

Expected: No lint errors.

- [ ] **Step 3: Smoke test — English**

```bash
pnpm dev
```

Visit each page (`/`, `/map`, `/analytics`) in English. Verify no broken strings, no `[missing]` markers, no layout breaks.

- [ ] **Step 4: Smoke test — French**

Click `FR` toggle. Visit each page. Verify all text is translated, dates use French formatting, no English strings leaked through.

- [ ] **Step 5: Verify cookie persistence**

Close and reopen the browser. The language should persist (cookie is set for 1 year).

- [ ] **Step 6: Verify Accept-Language fallback**

Clear the `NEXT_LOCALE` cookie. Set browser language to French (`fr`). Reload — the page should render in French automatically.

- [ ] **Step 7: Final commit**

If any fixes were needed during smoke testing, commit them:

```bash
git add -A
git commit -m "fix(i18n): smoke test fixes"
```
