# Copy to inventory.ini and set your VPS. The group name must stay [edwt]
# (the playbook targets `hosts: edwt`).

[edwt]
edwt-vps ansible_host=YOUR_VPS_IP ansible_user=root

# If you have a Host alias in ~/.ssh/config (with the right IdentityFile), use:
# edwt-vps ansible_host=my-vps-alias
